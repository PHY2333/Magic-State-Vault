---
task_id: 20260827-hgp-v3-pilot
status: manuscript_validated
target_files:
  - Notes/07-Lifted-Product Code/Hypergraph product code.md
---

# 目标

- 为 HGP 主笔记设计并起草短小 onboarding 单元 U01、U02。
- 让第一次接触 HGP 的读者先建立构造类别、输入／输出、CSS 对易问题和三项箭头的整体图景。
- 完整验证 Notes v3 从 mapping 到 manuscript audit 的阶段门、隔离和内部返修机制。

# 授权范围

- 读取目标文件、相关正式笔记、canonical/index 与已核对来源以完成 mapping。
- 只在本任务目录生成 v3 任务产物、`PACKETS/U01.md`、`PACKETS/U02.md`、`DRAFTS/U01.md`、`DRAFTS/U02.md`。
- Writer 只写 staged drafts，不修改正式文件。

# 非目标

- 不修改 `Notes/07-Lifted-Product Code/Hypergraph product code.md`。
- 不修改 `Notes/00-index.md`、`CANONICAL_KNOWLEDGE.md` 或其它正式笔记。
- 不设计 U03，不执行 integration，不运行到 `integrating` 或 `published`。
- 不展开 Kronecker blocks、输入矩阵尺寸、total degree、距离、qLDPC、LP 或 Künneth 用途。

# 当前阶段

- Design revision 3 已通过独立 Design Audit。
- Packet Builder 已编译并预检 `PACKETS/U01.md` 与 `PACKETS/U02.md`。
- 两个隔离 Writer 已分别接收且只能读取 `PACKETS/U01.md` 与 `PACKETS/U02.md`。
- U01/U02 draft revision 1 已完成；首次独立 Manuscript Audit 返回 `changes_required`，无 blocker。
- Packet Builder 已补齐真实 design-audit gate record；隔离 U02 Writer 已完成两项标题返修，形成 draft revision 2。
- Revision 2 已通过完整独立 Manuscript Audit；三项 revision 1 findings 全部闭合。
- 当前阶段：`manuscript_validated`。按 pilot 停止点停止，不执行 integration。
- design 内部返修次数：2。
- manuscript 内部返修次数：1。
