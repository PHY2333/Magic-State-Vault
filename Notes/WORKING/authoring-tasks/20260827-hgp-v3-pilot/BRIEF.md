# 学习目标

- 可观察表现 1：读者能说明 HGP 是从两张经典二进制校验矩阵构造 CSS 量子码的方法或由此得到的一类码。
- 可观察表现 2：读者能指出两张经典校验矩阵是输入，并区分输出的 \(H_X,H_Z\)。
- 可观察表现 3：读者能说明输出不能是任意两张矩阵，因为两类 checks 必须彼此对易。
- 可观察表现 4：读者能用
  \[
  C_2\xrightarrow{H_Z^T}C_1\xrightarrow{H_X}C_0
  \]
  说明“连续两步为零”就是 \(H_XH_Z^T=0\)，并理解这张整体图怎样承接自动对易问题。
- 可观察表现 5：读者读完 U01、U02 后不需要借助 wikilink、前置清单或维护边界补足开头含义。

# 目标材料

- 目标文件：`Notes/07-Lifted-Product Code/Hypergraph product code.md`。
- 本次只设计和起草 U01、U02，草稿保存到本任务目录的 `DRAFTS/`。
- 目标文件长期职责保持 `reference`，读者入口采用 `guided`。

# 当前真实问题

- 现有开头在说明 HGP 的读者落点后立即给出三个前置链接、Künneth 边界和维护性判断。
- CSS、输入／输出和自动对易之间缺少面向首次接触者的连续桥梁。
- 三项箭头目前在输入矩阵尺寸和二项复形之后才出现，不能先承担“为什么构造自动对易”的简化整体图景。

# 已有证据

- 用户明确要求把 HGP 类别、输入与输出作为第一次接触，因此这些能力标为 `unseen`。
- 用户提到 Künneth 名称，只支持 `named`；其用途不作能力假设，U01/U02 完全 omit。
- CSS 对易、经典校验矩阵行列角色、Kronecker 操作、chain/cochain 迁移和 \(\mathbb F_2\) 抵消没有直接能力证据，均标为 `unverified`。
- “名称见过”和“能操作”必须拆分记录；仓库中存在相关正式笔记不构成 learner evidence。

# 非目标

- 不把 onboarding 拆成独立 lesson 或新文件。
- 不设计 U03。
- 不在正文出现 Künneth、qLDPC、LP、距离、canonical、前置、不是前置、wikilink 串或流程语言。
- 不修改正式 HGP 文件、索引或 canonical。

# 可能需要用户决定

- 无。用户已经明确文件类型、入口模式、单元范围、阶段停止点和隔离要求。
