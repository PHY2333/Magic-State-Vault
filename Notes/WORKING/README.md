# Notes working area

当前活动写作任务只使用：

```text
Notes/WORKING/pro-tasks/<task-id>/
```

旧 `note-plans/`、`note-tasks/` 和 `authoring-tasks/` 已从活动树移除。它们仍可在迁移前创建的 archive tag 和 Git 历史中查看，不应重新恢复为活动工作流。

正式 Notes 不得链接本目录。
